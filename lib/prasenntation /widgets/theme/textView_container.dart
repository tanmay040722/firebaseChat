import 'package:flutter/material.dart';

class TextViewContainer extends StatelessWidget {
  const TextViewContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
